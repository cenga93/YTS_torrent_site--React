import React, { Component } from "react";
import { MainSearch } from "../../components";
class BrowseMovie extends Component {
  render() {
    return <MainSearch className={`main_search`} />;
  }
}
export default BrowseMovie;
